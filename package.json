{
  "name": "crmgold",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "description": "CRM for a gold trading business — investors, expenses, profit/loss, per-investor profit split.",
  "scripts": {
    "install:all": "npm install && npm --prefix server install && npm --prefix client install",
    "setup": "npm --prefix server run setup",
    "dev": "concurrently -k -n SERVER,CLIENT -c blue,green \"npm:dev:server\" \"npm:dev:client\"",
    "dev:server": "npm --prefix server run dev",
    "dev:client": "npm --prefix client run dev",
    "build": "npm --prefix client run build",
    "test": "npm --prefix server test",
    "start": "npm --prefix server run start",
    "vercel-build": "npm --prefix server run generate && npm --prefix client run build",
    "db:deploy": "npm --prefix server run deploy",
    "db:seed": "npm --prefix server run seed"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
